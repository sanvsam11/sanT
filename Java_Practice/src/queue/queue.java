package queue;

public class queue<t>
{
	node front,rear,c;
	public queue(t element){
		front=new node(element);
		rear=front;
	}
	public void enqueue(t element){
		if(front==null)new queue(element);
		else{
			
		}
	}
}
